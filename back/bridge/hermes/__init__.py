"""Hermes external-runtime bridge.

The package root deliberately performs no runtime bootstrap. Individual bridge
contributions stay independently importable by the application composition layer.
"""
